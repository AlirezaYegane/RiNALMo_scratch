embedding:
  num_embeddings: 0
  embedding_dim: 1280
  padding_idx: 0

transformer:
  embed_dim: 1280
  num_blocks: 33
  num_heads: 20
  use_rot_emb: true
  attn_qkv_bias: false
  transition_dropout: 0.0
  attention_dropout: 0.0
  residual_dropout: 0.0
  transition_factor: 4
  use_flash_attn: true

lm_mask_head:
  embed_dim: 1280
  alphabet_size: 0

token_dropout:
  active: true
  mask_ratio: 0.15
  mask_tkn_prob: 0.8
  mask_tkn_idx: 0
  pad_tkn_idx: 0
